import Messenger from '../../contexts/messenger.js';
// import { MessengerInterface } from '../contexts/messengerInterface.js';

export interface MessagesInterface {
    // messages?: Record<string, unknown>;
    messenger?: Messenger;
    // resourceConfig?: MessengerInterface;
}
